<%- include('partials/header') %>
<div class="empty-state">
  <div class="empty-icon">🔒</div>
  <h2>You don't have access<% if (serviceName) { %> to <%= serviceName %><% } %></h2>
  <p class="muted">You're signed in as <%= user.email %>, but this service hasn't been granted to you.
  Ask the administrator if you think you should have access.</p>
  <p><a class="btn btn-primary" href="/">Back to your services</a></p>
</div>
<%- include('partials/footer') %>
